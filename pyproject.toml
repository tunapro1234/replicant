[project]
name = "behave-lab"
version = "0.1.0"
description = "LLM-based replication of behavioral economics experiments"
requires-python = ">=3.10"
dependencies = [
    "edsl>=1.0",
]

[project.optional-dependencies]
analysis = ["pandas>=2.0", "matplotlib>=3.7"]
