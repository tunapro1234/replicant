"""
TEMPLATE — paper-specific parameters and known findings.

Replace this with the actual game parameters from your paper, and
fill in PAPER_FINDINGS with results you want to compare against.
"""

# ── Game parameters ─────────────────────────────────────────────────
# These are the parameters that define the game.
# Examples below are placeholders — replace with your paper's values.

ENDOWMENT = 10           # tokens given to each participant
GROUP_SIZE = 4           # number of players per group
# MPCR = 0.4             # marginal per capita return (for VCM games)
# OFFER_RANGE = (0, 10)  # for ultimatum/dictator games
# ROUNDS = 1             # number of rounds (1 for single-shot)


# ── Known findings (for comparison to original paper) ───────────────
# Each entry: "metric_name": (expected_value, "human-readable description")

PAPER_FINDINGS = {
    # "average_offer": (4.5, "average offer in ultimatum game"),
    # "rejection_rate_low": (0.5, "rejection rate for offers below 30%"),
}
